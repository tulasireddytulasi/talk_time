enum MainScreens { movies, tvSeries, celebrities }

enum SubScreens { popular, topRated, upcoming, nowPlaying }

enum Types { text, imageText, image, video, videoText, audio }

enum MessStatus {sent, notSent}

enum PlatformType {
  mobile,
  desktop,
  web,
  android,
  ios,
  macos,
  windows,
  linux,
  unknown,
}
